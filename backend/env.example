# Preferred keys
PROVIDER=gemini          # options: gemini | openai | mock
GEMINI_API_KEY=
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Legacy compatible keys (still work)
MODEL=gemini             # gemini|openai|mock
API_KEY=                 # used if MODEL=gemini or MODEL=openai

HOST=0.0.0.0
PORT=8000
ALLOWED_ORIGINS=*
